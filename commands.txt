git config --global user.name "username"

git config --global user.email "useremail"

git init => for initializing git in pwd

git status => list all untracked files

git add -A => adding all changes to staging area

git commit -m "commit message" => adding staged files to local respiratory

git remote add origin https://github.com/Shinjul-V/gitbasicmlbatch.git

git remote -v 